syntax = "proto3";
option java_package = "com.nettysample.withprotobuf.proto";
option java_outer_classname = "UserModel";// 生成的外部类名，同时也是文件名

// protobuf 使用 message 管理数据
message User {// 会在 UserModel 里面生成一个内部类 User，即是真正发送的 POJO 对象
  int32 id = 1;
  string name = 2;
}
